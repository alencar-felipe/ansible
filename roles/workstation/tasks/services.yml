- name: Enable gdm service
  service:
    name: gdm
    enabled: yes
    
- name: Enable NetworkManager service
  service:
    name: NetworkManager
    enabled: yes
    state: started
    
- name: Enable cups service
  service:
    name: cups
    enabled: yes
    state: started 
